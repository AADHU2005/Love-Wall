/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'i.imgur.com',
      'i.pinimg.com',
      // Add more domains as needed
    ],
  },
};

module.exports = nextConfig;
